{
  "name": "domain-search",
  "version": "0.1.0",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outdir=dist --platform=node --format=cjs",
    "test": "npm run build && ava"
  },
  "devDependencies": {
    "ava": "^5.3.1",
    "esbuild": "^0.19.5",
    "typescript": "^5.3.3"
  }
}
